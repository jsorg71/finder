
OBJS=finder_gui.obj

CFLAGS=/O2 /W3 /I.. $(FINDER_CFLAGS)

LDFLAGS=$(FINDER_LDFLAGS)

LIBS=$(FINDER_LIBS)

all: finder.exe

finder.exe: $(OBJS) ../finder.lib
	link /out:finder.exe $(OBJS) $(LDFLAGS) ../finder.lib $(LIBS)

clean:
	del finder.exe $(OBJS)
